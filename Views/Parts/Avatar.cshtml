@if ((bool)Model.HasAvatar)
{
    @Html.Image((string)Model.ImageUrl, T("{0}'s avatar", (string)Model.UserName).ToString(), new Dictionary<string, object> { { "class", "piedone-avatars avatar" } })
}